{
  "name": "e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests using Playwright",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5"
  }
}
